/*
 * Problem  : [Problem Name]
 * Source   : LeetCode #XX / GFG / Coding Ninjas
 * Step     : Striver A2Z - Step XX - Lec XX
 * Approach : Brute Force / Better / Optimal
 * Time     : O(n)
 * Space    : O(1)
 * Date     : DD-MM-YYYY
 */

#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    // ── Brute Force ─────────────────────────────
    // Time: O(n²) | Space: O(1)
    
    // ── Optimal ─────────────────────────────────
    // Time: O(n) | Space: O(1)
};

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    // Test cases
    Solution sol;
    
    return 0;
}
